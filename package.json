{
  "name": "constraint-inference",
  "version": "0.2.0",
  "description": "Reverse-engineers constraint parameters from user override patterns",
  "main": "src/index.js",
  "scripts": {
    "start": "ts-node src/index.ts",
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "ts-node": "^10.9.0",
    "typescript": "^5.3.0",
    "vitest": "^4.1.6"
  }
}
